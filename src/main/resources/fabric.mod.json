{
  "schemaVersion": 1,
  "id": "optimizer",
  "name": "Walksy Crystal Optimizer",
  "version": "${version}",
  "environment": "client",
  "entrypoints": {
    "client": [
      "walksy.optimizer.Optimizer"
    ]
  },
  "mixins": [
    "optimizer.mixins.json"
  ],
  "depends": {
    "fabricloader": ">=0.16.0",
    "minecraft": "~1.21.1",
    "fabric-api": "*"
  }
}